# n8n_python_auto-scaling-health-checks
This script will create a provisioned VM in qemu-kvm, that will host auto-scaling n8n and python containers with a health-check service that outputs emergencies to mail, slack and sms.
